version: 1
backend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - npm ci --include=dev
    build:
      commands:
        - npx ampx pipeline-deploy --branch main --app-id d2h8rnvkxcy3jc
  cache:
    paths:
      - node_modules/**/*
      - .npm/**/*
